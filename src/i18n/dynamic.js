/* AUTO-GENERATED — dynamic-string dictionary. Simulated command OUTPUT stays
   English on purpose (real docker/kubectl speak English); teaching notes translate. */
export const KO_STR=[
 ["Simulated Docker engine ready. Type 'help' or start with: docker pull nginx","시뮬레이션 Docker 엔진 준비 완료. 'help'를 입력하거나 docker pull nginx 로 시작해 보세요. (명령 출력은 실제 도구처럼 영어로 표시됩니다)"],
 ["kubectl configured for sim-cluster (3 nodes). Type 'help' or start with: kubectl get nodes","sim-cluster(3개 노드)에 kubectl 연결 완료. 'help'를 입력하거나 kubectl get nodes 로 시작해 보세요."],
 ["Commands: docker pull|run|ps|stop|start|rm|rmi|images|logs|exec|inspect, nvidia-smi, curl, clear\nExample: docker run -d -p 8080:80 --name web nginx","사용 가능: docker pull|run|ps|stop|start|rm|rmi|images|logs|exec|inspect, nvidia-smi, curl, clear\n예시: docker run -d -p 8080:80 --name web nginx"],
 ["(This is the host — it has no GPU. But a container CAN have one… see Module 5 and try: docker run --gpus all pytorch/pytorch nvidia-smi)","(여기는 호스트입니다 — GPU가 없어요. 하지만 컨테이너는 가질 수 있죠… GPU 모듈을 본 뒤 시도: docker run --gpus all pytorch/pytorch nvidia-smi)"],
 [/← your container answered on port (\d+) 🎉/g,"← 컨테이너가 $1 포트에서 응답했습니다 🎉"],
 ["curl: (7) Failed to connect to localhost — is a container publishing that port with -p?","curl: (7) localhost 연결 실패 — 해당 포트를 -p 로 공개한 컨테이너가 있나요?"],
 ["(this simulated host only speaks docker — try 'help')","(이 시뮬레이션 호스트는 docker 명령만 이해합니다 — 'help')"],
 [/([\w./-]+) ran its command and exited — containers live only as long as their main process\./g,"$1 은(는) 명령을 실행하고 종료되었습니다 — 컨테이너는 메인 프로세스가 살아있는 동안만 살아있습니다."],
 ["(attached — in real life Ctrl-C here; simulator detaches for you. Tip: use -d)","(포그라운드 연결됨 — 실제라면 여기서 Ctrl-C. 시뮬레이터가 대신 분리했습니다. 팁: -d 를 쓰세요)"],
 ["(exited containers are hidden — use docker ps -a)","(종료된 컨테이너는 숨겨집니다 — docker ps -a 를 쓰세요)"],
 ["The toolkit injected the host driver + /dev/nvidia0 into the container. The 7GB image carries the CUDA runtime; the driver came from the host.","툴킷이 호스트의 드라이버와 /dev/nvidia0 를 컨테이너에 주입했습니다. 7GB 이미지에는 CUDA 런타임이 들어있고, 드라이버는 호스트에서 왔습니다."],
 ["No --gpus flag → no driver injection → container is blind to the GPU. Retry with --gpus all","--gpus 플래그 없음 → 드라이버 주입 없음 → 컨테이너는 GPU를 볼 수 없습니다. --gpus all 로 다시 시도해 보세요"],
 ["That's the container's OWN filesystem (its image layers) — not your laptop's.","컨테이너 자신의 파일시스템(이미지 레이어)입니다 — 노트북의 파일시스템이 아니에요."],
 ["PID 1 inside! The PID namespace hides all other host processes.","컨테이너 안에서는 PID 1! PID 네임스페이스가 다른 모든 호스트 프로세스를 숨깁니다."],
 ["(interactive shells aren't simulated — try: docker exec ","(대화형 셸은 시뮬레이션되지 않습니다 — 시도: docker exec "],
 ["(simulated) executed '","(시뮬레이션) 실행됨: '"],
 ["' inside ","' — 대상 컨테이너: "],
 ["This is the Kubernetes lab — here you speak kubectl, not docker. K8s tells each node's container runtime what to run; you never docker-run manually.","여기는 쿠버네티스 실습입니다 — docker가 아니라 kubectl로 말하세요. K8s가 각 노드의 컨테이너 런타임에 무엇을 실행할지 지시하며, 직접 docker run 할 일은 없습니다."],
 [/→ Deployment written to etcd\. ReplicaSet controller creates (\d+) pod\(s\); scheduler assigns nodes; kubelets start containers\. Watch the cluster view →/g,"→ Deployment가 etcd에 기록되었습니다. ReplicaSet 컨트롤러가 파드 $1개를 생성하고, 스케줄러가 노드를 배정하며, kubelet이 컨테이너를 시작합니다. 오른쪽 클러스터 뷰를 보세요 →"],
 ["Heads up: workers fit 4 pods each — extras will sit Pending. This is exactly what happens in real clusters when capacity runs out (then autoscalers add nodes).","참고: 워커당 파드 4개까지입니다 — 초과분은 Pending 상태로 대기합니다. 실제 클러스터에서 용량이 부족할 때 벌어지는 일 그대로입니다(그때 오토스케일러가 노드를 추가하죠)."],
 ["Now watch the cluster view: desired ≠ actual, so a replacement pod appears in ~1s with a NEW name. Pods are cattle, not pets.","이제 클러스터 뷰를 보세요: 원하는 상태 ≠ 실제 상태이므로 약 1초 안에 새 이름의 대체 파드가 나타납니다. 파드는 반려동물이 아니라 가축입니다."],
 [/The Service gets a stable virtual IP \+ DNS name \((.+?)\)\. Pods come and go; the Service always routes to whichever are alive\./g,"Service는 고정 가상 IP와 DNS 이름($1)을 얻습니다. 파드는 생겼다 사라지지만 Service는 항상 살아있는 파드로 라우팅합니다."],
 ["Rolling update begins: new-image pods (purple glow) are created one at a time while old ones terminate — zero downtime. Watch →","롤링 업데이트 시작: 새 이미지 파드(보라색 광)가 하나씩 생성되고 기존 파드는 종료됩니다 — 무중단 배포. 오른쪽을 보세요 →"],
 /* module 3 narrator */
 ["Already deployed — try the other buttons, or Reset.","이미 배포되어 있어요 — 다른 버튼을 누르거나 초기화하세요."],
 ["📜 You → API server: <code>Deployment web, replicas: 3</code>. Scheduler assigns each pod to the least-loaded node; kubelets pull the image and start containers (yellow → green).","📜 나 → API 서버: <code>Deployment web, replicas: 3</code>. 스케줄러가 가장 여유 있는 노드에 파드를 배정하고, kubelet이 이미지를 받아 컨테이너를 시작합니다(노랑 → 초록)."],
 ["No running pods to kill — deploy first.","죽일 파드가 없어요 — 먼저 배포하세요."],
 ["💥 A pod just died (node crash, OOM, whatever). Watch: the ReplicaSet controller sees actual=2 &lt; desired=3 and creates a replacement within seconds. <b>You did nothing.</b>","💥 파드 하나가 방금 죽었습니다(노드 장애, OOM, 뭐든). 보세요: ReplicaSet 컨트롤러가 실제=2 &lt; 원함=3 을 감지하고 몇 초 안에 대체 파드를 만듭니다. <b>당신은 아무것도 하지 않았습니다.</b>"],
 ["Deploy first.","먼저 배포하세요."],
 ["Already at 6.","이미 6개입니다."],
 ["📈 <code>kubectl scale --replicas=6</code> — one line. Desired state changed from 3→6, controller creates 3 more, scheduler spreads them. Imagine doing this across 500 machines by hand.","📈 <code>kubectl scale --replicas=6</code> — 단 한 줄. 원하는 상태가 3→6으로 바뀌자 컨트롤러가 3개를 더 만들고 스케줄러가 분산 배치합니다. 이걸 500대 머신에서 손으로 한다고 상상해 보세요."],
 ["Everything is already v2 — Reset to replay.","이미 전부 v2입니다 — 초기화 후 다시 재생하세요."],
 ["🔄 <code>kubectl set image ... app=web:v2</code> — a <b>rolling update</b>: replace pods one at a time (purple = v2), so some replicas always serve traffic. Zero downtime, and <code>rollout undo</code> can reverse it.","🔄 <code>kubectl set image ... app=web:v2</code> — <b>롤링 업데이트</b>: 파드를 한 번에 하나씩 교체하므로(보라 = v2) 항상 일부 레플리카가 트래픽을 처리합니다. 무중단이며 <code>rollout undo</code>로 되돌릴 수 있습니다."],
 ["Cluster reset. Start with 1 · Deploy.","클러스터 초기화 완료. 1 · 배포 부터 시작하세요."],
 /* module 1 lifecycle + layers */
 ["<b>Created</b>: filesystem prepared, process NOT started yet. (docker run = create + start in one go.)","<b>Created</b>: 파일시스템은 준비됐지만 프로세스는 아직 시작 전. (docker run = create + start 를 한 번에.)"],
 ["<b>Running</b>: the container's main process (PID 1) is alive. When PID 1 exits, the container exits.","<b>Running</b>: 컨테이너의 메인 프로세스(PID 1)가 살아있습니다. PID 1이 종료되면 컨테이너도 종료됩니다."],
 ["<span style='color:var(--red)'>Error: no such container — create it first.</span>","<span style='color:var(--red)'>오류: 그런 컨테이너가 없습니다 — 먼저 create 하세요.</span>"],
 ["<b>Exited</b>: docker sent SIGTERM, waited 10s, then SIGKILL. Filesystem still exists — logs readable, restartable.","<b>Exited</b>: docker가 SIGTERM을 보내고 10초 기다린 뒤 SIGKILL. 파일시스템은 남아있어 로그를 읽거나 재시작할 수 있습니다."],
 ["<span style='color:var(--red)'>Error: container is not running.</span>","<span style='color:var(--red)'>오류: 컨테이너가 실행 중이 아닙니다.</span>"],
 ["<b>Removed</b>: writable layer deleted. Anything not in a volume is gone forever. The image is untouched.","<b>Removed</b>: 쓰기 레이어가 삭제되었습니다. 볼륨에 없던 데이터는 영원히 사라집니다. 이미지는 그대로입니다."],
 ["<span style='color:var(--red)'>Error: cannot remove a running container — stop it first (or use -f).</span>","<span style='color:var(--red)'>오류: 실행 중인 컨테이너는 삭제할 수 없습니다 — 먼저 stop 하세요(또는 -f).</span>"],
 ["<span style='color:var(--red)'>Error: no such container.</span>","<span style='color:var(--red)'>오류: 그런 컨테이너가 없습니다.</span>"],
 ["FROM pulls the base image — you start from someone else's layers.","FROM은 베이스 이미지를 가져옵니다 — 남이 만든 레이어 위에서 시작하는 거죠."],
 ["RUN executes a command and snapshots the filesystem diff as a new layer.","RUN은 명령을 실행하고 파일시스템 변경분을 새 레이어로 스냅샷합니다."],
 ["COPY adds your files — tiny layer. Order matters: put rarely-changing steps first so the cache survives code edits.","COPY는 내 파일을 추가합니다 — 아주 작은 레이어. 순서가 중요합니다: 잘 안 바뀌는 단계를 앞에 두면 코드를 고쳐도 캐시가 살아남습니다."],
 ["CMD adds no files, only metadata: the default command to run.","CMD는 파일을 추가하지 않고 메타데이터만 남깁니다: 기본 실행 명령."],
 ["docker run adds ONE thin writable layer. 100 containers from one image share the read-only layers — that's the space magic.","docker run은 얇은 쓰기 레이어 하나만 추가합니다. 이미지 하나로 만든 컨테이너 100개가 읽기 전용 레이어를 공유합니다 — 용량 마법의 비밀."],
 ["Each Dockerfile instruction creates one layer. Layers are cached & shared between images — that's why pulls are fast.","Dockerfile 명령 하나가 레이어 하나를 만듭니다. 레이어는 캐시되고 이미지끼리 공유됩니다 — pull이 빠른 이유죠."],
 ["✓ Built + running","✓ 빌드 + 실행 완료"],
 ["▶ docker build — next step","▶ docker build — 다음 단계"],
 /* state panels */
 ["none — try docker pull nginx","없음 — docker pull nginx 를 해보세요"],
 ["none running","실행 중인 것 없음"],
 /* k8s render */
 [/(\d+)\/4 pods/g,"$1/4 파드"],
 [">worker</span>",">워커</span>"],
 /* gpu sim */
 [">free<",">여유<"],
 [/(\d+)\/4 free/g,"$1/4 여유"],
 ["no GPUs — device plugin not present","GPU 없음 — device plugin 미설치"],
 ["runs your web apps, DBs, CI…","웹 앱, DB, CI 등을 실행…"],
 ["→ <b>Pending forever</b>.","→ <b>영원히 Pending</b>."],
 [" scheduled → "," 스케줄됨 → "],
 ["<b>Lesson:</b> a pod's GPU request must fit on ONE node — you can't stitch 4+4 across nodes into one pod. Jobs bigger than a node become <i>multiple</i> pods doing distributed training (NCCL/torchrun).","<b>교훈:</b> 파드의 GPU 요청은 노드 하나에 들어가야 합니다 — 노드 두 개의 4+4를 파드 하나로 이어붙일 수 없습니다. 노드보다 큰 작업은 <i>여러 개의</i> 파드가 분산 학습(NCCL/torchrun)을 하는 형태가 됩니다."],
 ["<b>Lesson:</b> GPUs are never oversubscribed — the pod queues until another job finishes (or an autoscaler adds a GPU node, at $$$).","<b>교훈:</b> GPU는 절대 초과 할당되지 않습니다 — 다른 작업이 끝나거나 오토스케일러가 GPU 노드를 추가할 때까지($$$) 파드는 줄을 섭니다."],
 ["(bin-packing: chose the node with the least free GPUs that still fits, keeping big slots open elsewhere).","(빈 패킹: 들어갈 수 있으면서 여유 GPU가 가장 적은 노드를 골라, 큰 자리를 다른 곳에 남겨둡니다.)"],
 ["Took a whole node's GPUs — a 4-GPU job blocks four 1-GPU jobs.","노드의 GPU 전부를 차지했습니다 — 4-GPU 작업 하나가 1-GPU 작업 네 개를 막습니다."],
 ["Cluster reset.","클러스터 초기화 완료."],
 /* mig */
 ["Enable MIG","MIG 켜기"],["Disable MIG","MIG 끄기"],
 ["1 × whole GPU — nvidia.com/gpu: 1","1 × GPU 통째로 — nvidia.com/gpu: 1"],
 ["<b>MIG enabled:</b> the A100 now shows up as <code>nvidia.com/mig-1g.10gb: 7</code>. Seven small pods (notebooks, inference services) can share one physical GPU with hardware isolation. Watch them land:","<b>MIG 켜짐:</b> A100이 이제 <code>nvidia.com/mig-1g.10gb: 7</code> 로 표시됩니다. 작은 파드 7개(노트북, 추론 서비스)가 하드웨어 격리 상태로 물리 GPU 하나를 나눠 씁니다. 파드가 착륙하는 걸 보세요:"],
 ["<b>MIG</b> (Multi-Instance GPU, A100/H100+): hardware-partitions one GPU into up to 7 isolated instances with dedicated memory/compute. Each appears as its own schedulable resource (<code>nvidia.com/mig-1g.10gb</code>). Strong isolation — safe for multi-tenant clusters.","<b>MIG</b>(Multi-Instance GPU, A100/H100+): GPU 하나를 전용 메모리/연산을 가진 최대 7개의 격리 인스턴스로 하드웨어 분할합니다. 각각이 독립적인 스케줄링 자원(<code>nvidia.com/mig-1g.10gb</code>)으로 나타납니다. 격리가 강력해 멀티테넌트 클러스터에 안전합니다."],
 [/pod (\d)$/g,"파드 $1"],
 /* msbuild */
 ["naive single-stage:","단일 스테이지:"],["multi-stage final:","멀티 스테이지 최종:"],
 /* quiz footer */
 [/^Score: /g,"점수: "],
 ["🏆 — go break a real cluster (kind or minikube) next!","🏆 — 이제 진짜 클러스터(kind/minikube)를 부수러 가세요!"],
 ["👏 solid — reread the ones you missed.","👏 탄탄합니다 — 틀린 문제만 다시 읽어보세요."],
 ["— revisit modules 1–5 and try again.","— 모듈들을 복습하고 다시 도전하세요."],
 /* k8s lab v2 — teaching notes */
 ["Troubleshooting flow: kubectl describe pod NAME (read Events) → kubectl logs NAME. ImagePullBackOff = bad image name/tag; CrashLoopBackOff = the process inside keeps dying.","트러블슈팅 순서: kubectl describe pod 이름 (Events 읽기) → kubectl logs 이름. ImagePullBackOff = 이미지 이름/태그 오류; CrashLoopBackOff = 안의 프로세스가 계속 죽는 중."],
 [/Heads up: the registry doesn't know "(.+?)" — the Deployment is accepted anyway \(the API server doesn't check images\)\. The kubelet will fail to pull it\. Watch for ImagePullBackOff\./g,"참고: 레지스트리에 \"$1\" 이미지는 없습니다 — 그래도 Deployment는 받아들여집니다(API 서버는 이미지를 검사하지 않아요). kubelet이 pull에 실패할 겁니다. ImagePullBackOff를 지켜보세요."],
 ["Secrets are only base64-encoded, not encrypted — see for yourself: kubectl get secret ","Secret은 암호화가 아니라 base64 인코딩일 뿐입니다 — 직접 확인: kubectl get secret "],
 ["This is a bare pod — no controller owns it. If it dies or you delete it, nothing brings it back. Deployments exist for a reason.","이건 생(bare) 파드입니다 — 어떤 컨트롤러도 소유하지 않아요. 죽거나 삭제되면 아무도 되살리지 않습니다. Deployment가 존재하는 이유죠."],
 ["Bare pod: nothing owned it, so nothing will recreate it. Gone is gone.","생 파드: 소유자가 없으므로 아무것도 다시 만들지 않습니다. 사라지면 끝."],
 ["Undo is just another rolling update — to the previous template. Check kubectl rollout history: the old image comes back as a NEW revision.","undo도 그저 또 한 번의 롤링 업데이트입니다 — 이전 템플릿으로요. kubectl rollout history를 보세요: 예전 이미지가 '새' 리비전으로 돌아옵니다."],
 ["Labels changed. Anything selecting on labels reacts instantly: Services route (or stop routing) to this pod, and if it no longer matches its ReplicaSet's selector the controller replaces it.","레이블이 바뀌었습니다. 레이블로 선택하는 모든 것이 즉시 반응합니다: Service 라우팅이 바뀌고, ReplicaSet 셀렉터와 안 맞게 되면 컨트롤러가 대체 파드를 만듭니다."],
 ["New pods that don't tolerate this taint won't schedule here. (NoSchedule doesn't evict pods already running — NoExecute would.)","이 taint를 견디지(tolerate) 못하는 새 파드는 여기 스케줄되지 않습니다. (NoSchedule은 이미 실행 중인 파드를 쫓아내지 않아요 — NoExecute라면 쫓아냅니다.)"],
 ["Drain = cordon + evict. Deployment-owned pods get recreated on other nodes; bare pods are just gone. This is how you prep a node for maintenance or upgrade.","drain = cordon + 축출(evict). Deployment 소속 파드는 다른 노드에 재생성되고, 생 파드는 그냥 사라집니다. 노드 점검/업그레이드 준비가 바로 이겁니다."],
 ["Endpoints is empty — no ready pod matches this selector. Check: kubectl get pods --show-labels (labels vs selector), and pod readiness.","Endpoints가 비어 있습니다 — 이 셀렉터에 맞는 ready 파드가 없어요. 확인: kubectl get pods --show-labels (레이블 vs 셀렉터), 그리고 파드 readiness."],
 [/DNS: no Service named "(.+?)" in namespace "(.+?)"\. Service DNS = name\.namespace\.svc\.cluster\.local\./g,"DNS: 네임스페이스 \"$2\"에 \"$1\" 이라는 Service가 없습니다. Service DNS = 이름.네임스페이스.svc.cluster.local."],
 [/The Service exists but listens on port (\d+), not (\d+)\./g,"Service는 있지만 $2 이 아니라 $1 포트에서 듣고 있습니다."],
 [/The Service has NO endpoints — its selector matches no ready pod\. Debug: kubectl describe svc (.+?) ; kubectl get pods --show-labels/g,"Service에 endpoint가 없습니다 — 셀렉터에 맞는 ready 파드가 없어요. 디버그: kubectl describe svc $1 ; kubectl get pods --show-labels"],
 [/Opened (.+?) in the Manifests editor → edit it, then: kubectl apply -f (.+)/g,"$1 을(를) 매니페스트 편집기에서 열었습니다 → 수정 후: kubectl apply -f $2"],
 [/\(wrote (\d+) lines to (.+?) — see the Manifests editor →\)/g,"($2 에 $1줄 저장 — 매니페스트 편집기를 보세요 →)"],
 [/← answered by (.+?) \((.+?)\)/g,"← $1 ($2) 이(가) 응답했습니다"],
 ["(the container's main process keeps dying — that's what CrashLoopBackOff means)","(컨테이너의 메인 프로세스가 계속 죽고 있습니다 — CrashLoopBackOff의 의미가 바로 이것)"],
 /* ckad drills (m11) — teaching notes */
 ["The values above were injected at container START from the ConfigMap/Secret. Note: secret values arrive as plain text inside the container — base64 is transport encoding, not encryption.","위 값들은 컨테이너가 시작될 때 ConfigMap/Secret에서 주입되었습니다. 참고: Secret 값은 컨테이너 안에서 평문으로 도착합니다 — base64는 전송용 인코딩이지 암호화가 아니에요."],
 ["Decoded! A mounted Secret appears as a plain-text file in the container. Anyone who can exec into the pod (or read etcd) can read it — that's why RBAC and encryption-at-rest matter.","디코딩 완료! 마운트된 Secret은 컨테이너 안에서 평문 파일로 보입니다. 파드에 exec할 수 있는(혹은 etcd를 읽을 수 있는) 누구나 읽을 수 있죠 — RBAC과 저장 시 암호화(encryption-at-rest)가 중요한 이유입니다."],
 [/This mount serves the keys of (.+?) as files: (.*)/g,"이 마운트는 $1 의 키들을 파일로 제공합니다: $2"],
 /* cka drills (m12) — scheduler & rbac teaching notes */
 ["A Role is only a list of allowed verb × resource pairs in ONE namespace. Nothing changes until a RoleBinding attaches it to a subject.","Role은 한 네임스페이스 안에서 허용되는 동사 × 리소스 목록일 뿐입니다. RoleBinding이 주체에 붙여주기 전까지는 아무것도 달라지지 않아요."],
 ["A ClusterRole is namespace-less. Bind it with a ClusterRoleBinding for cluster-wide power, or with a RoleBinding to grant it inside just one namespace.","ClusterRole에는 네임스페이스가 없습니다. ClusterRoleBinding으로 묶으면 클러스터 전체 권한이 되고, RoleBinding으로 묶으면 한 네임스페이스 안에서만 부여됩니다."],
 ["RBAC wiring complete: subject (who) ↔ role (which verbs on which resources). Verify it with: kubectl auth can-i VERB RESOURCE --as=system:serviceaccount:NS:NAME -n NS","RBAC 배선 완료: 주체(누가) ↔ Role(어떤 리소스에 어떤 동사). 검증: kubectl auth can-i 동사 리소스 --as=system:serviceaccount:NS:NAME -n NS"],
 ["You are cluster-admin in this sim, so the answer is always yes. Impersonate a subject to test RBAC: --as=system:serviceaccount:NS:NAME (or --as=USER).","이 시뮬레이터에서 당신은 cluster-admin이라 답은 항상 yes입니다. RBAC을 시험하려면 주체로 가장하세요: --as=system:serviceaccount:NS:NAME (또는 --as=USER)."],
 ["RBAC is deny-by-default: no Role/ClusterRole bound to this subject allows that verb on that resource (in this namespace). Grant it with a Role + RoleBinding.","RBAC은 기본 거부입니다: 이 주체에 묶인 어떤 Role/ClusterRole도 (이 네임스페이스에서) 그 리소스에 그 동사를 허용하지 않습니다. Role + RoleBinding으로 부여하세요."],
 [/\(Also: ServiceAccount (.+?) doesn't exist — impersonation still evaluates, but pods can't run as it\.\)/g,"(참고: ServiceAccount $1 은(는) 아직 없습니다 — 가장(impersonation) 평가는 되지만, 파드가 그 계정으로 실행될 수는 없어요.)"],
 /* networking drills (m13) — netpol / ingress / gateway teaching notes */
 ["NetworkPolicies select PODS (podSelector), not Services, and they are allow-lists: the moment any policy selects a pod, all traffic in that direction not explicitly allowed is dropped. Test it: kubectl exec POD -- wget -qO- SVC:PORT","NetworkPolicy는 Service가 아니라 파드를(podSelector) 선택하며, 허용 목록입니다: 어떤 정책이든 파드를 선택하는 순간 그 방향에서 명시적으로 허용되지 않은 트래픽은 전부 차단됩니다. 시험해 보세요: kubectl exec 파드 -- wget -qO- 서비스:포트"],
 [/Blocked by NetworkPolicy "(.+?)" \((\w+)\)\. DNS resolved and the endpoints are fine — the CNI is silently dropping the packets, which is why policy problems look like timeouts, not refusals\. Allow-list rule: once any policy selects a pod, only explicitly allowed traffic gets through\./g,"NetworkPolicy \"$1\"($2)에 의해 차단되었습니다. DNS도 endpoint도 멀쩡합니다 — CNI가 조용히 패킷을 버리고 있어서, 정책 문제는 거부(refused)가 아니라 타임아웃으로 보입니다. 허용 목록 규칙: 어떤 정책이든 파드를 선택하면 명시적으로 허용된 트래픽만 통과합니다."],
 ["An Ingress is only routing RULES — an ingress-controller pod does the actual proxying (the sim plays that part). Test it like an external client: curl http://HOST/PATH","Ingress는 라우팅 규칙일 뿐입니다 — 실제 프록시는 ingress-controller 파드가 담당하죠(이 시뮬레이터가 그 역할을 대신합니다). 외부 클라이언트처럼 시험하세요: curl http://호스트/경로"],
 [/no Ingress rule or HTTPRoute matches host "(.+?)" path "(.+?)" — the edge only answers for hosts\/paths a rule claims\. Check: kubectl get ingress ; kubectl get httproute/g,"호스트 \"$1\" 경로 \"$2\"에 맞는 Ingress 규칙이나 HTTPRoute가 없습니다 — 입구는 규칙이 선언한 호스트/경로에만 응답합니다. 확인: kubectl get ingress ; kubectl get httproute"],
 [/^(.+?) matched, but (.+?)\. Routing rule ✓ — now debug the Service\/pods behind it\./gm,"$1 이(가) 매칭됐지만, $2. 라우팅 규칙은 ✓ — 이제 그 뒤의 Service/파드를 디버그하세요."],
 ["Gateway = the listener half of routing (class, port, hostname). Routes attach to it separately: an HTTPRoute names this Gateway in spec.parentRefs — role separation Ingress never had.","Gateway는 라우팅의 리스너 절반입니다(클래스, 포트, 호스트네임). 라우트는 따로 붙습니다: HTTPRoute가 spec.parentRefs에서 이 Gateway를 지명하죠 — Ingress에는 없던 역할 분리입니다."],
 [/Heads up: no GatewayClass named "(.+?)" exists — the Gateway stays un-Programmed \(kubectl get gatewayclass\)\./g,"참고: \"$1\" 이라는 GatewayClass가 없습니다 — Gateway는 Programmed 상태가 되지 못합니다 (kubectl get gatewayclass)."],
 ["Weighted backendRefs split traffic — a built-in canary. curl the route a few times and count who answers.","가중치 backendRefs는 트래픽을 나눕니다 — 내장 카나리죠. 라우트에 curl을 몇 번 날려서 누가 응답하는지 세어 보세요."],
 [/← (.+?) matched (.+?) → Service\/(.+?) → (.+)/g,"← $1 → $2 매칭 → Service/$3 → $4"],
 /* cluster-ops drills (m14) — drain/PDB, kubeadm upgrade, etcd teaching notes */
 ["A PDB doesn't create or protect pods by itself — it only makes the eviction API (kubectl drain) refuse to go below the budget. Watch it: kubectl get pdb (ALLOWED DISRUPTIONS is computed live).","PDB는 스스로 파드를 만들거나 지키지 않습니다 — 축출 API(kubectl drain)가 예산 아래로 내려가기를 거부하게 만들 뿐이죠. 지켜보세요: kubectl get pdb (ALLOWED DISRUPTIONS는 실시간 계산됩니다)."],
 ["Allowed disruptions is 0 — every eviction (kubectl drain) that hits a covered pod will be refused until more matching pods are Ready (scale up) or the budget is loosened.","허용 중단 수가 0입니다 — 대상 파드를 건드리는 모든 축출(kubectl drain)은 매칭되는 Ready 파드가 늘어나거나(스케일 업) 예산이 느슨해질 때까지 거부됩니다."],
 [/PDB "(.+?)" (requires \d+ available|allows \d+ unavailable) and only (\d+) matching pod\(s\) are Ready — allowed disruptions is (\d+)\. Real drain retries forever; the sim gives up\. Make room \(scale the workload up, wait for Ready\) and drain again — the node STAYS cordoned meanwhile\./g,"PDB \"$1\"의 조건($2)에 비해 Ready 상태의 매칭 파드가 $3개뿐이라 허용 중단 수는 $4입니다. 실제 drain은 무한 재시도하지만 시뮬레이터는 포기합니다. 여유를 만들고(워크로드 스케일 업 후 Ready 대기) 다시 drain 하세요 — 그동안 노드는 cordon 상태로 유지됩니다."],
 ["Host commands run ON a node, not in the exam terminal. SSH into one first: ssh control-plane (then 'exit' to come back).","호스트 명령은 시험 터미널이 아니라 노드 위에서 실행됩니다. 먼저 ssh로 들어가세요: ssh control-plane ('exit'로 복귀)."],
 [/You are ON the node now: kubeadm, apt-get, systemctl(.*?) work here\. 'exit' returns to the exam terminal\./g,"이제 노드 위에 있습니다: kubeadm, apt-get, systemctl$1 을 여기서 쓸 수 있습니다. 'exit'로 시험 터미널로 돌아갑니다."],
 [", etcdctl/etcdutl and openssl (the pki files live here)",", etcdctl/etcdutl, openssl(pki 파일이 여기 있습니다)"],
 [/The kubelet BINARY is now (.+?), but the running process is still the old one — systemctl restart kubelet makes it take effect\./g,"kubelet 바이너리는 이제 $1이지만, 실행 중인 프로세스는 여전히 예전 것입니다 — systemctl restart kubelet 으로 반영하세요."],
 [/This kubeadm \(v(.+?)\) can only plan upgrades up to its own version — install the new one first: apt-get install -y kubeadm=(.+?)-1\.1, then plan again\./g,"이 kubeadm(v$1)은 자기 버전까지만 계획할 수 있습니다 — 먼저 새 kubeadm을 설치하세요: apt-get install -y kubeadm=$2-1.1, 그리고 다시 plan."],
 [/Check kubectl get nodes — the VERSION column still shows the OLD version! It reports each node's kubelet, and kubeadm does not touch kubelets: apt-get install -y kubelet=(.+?)-1\.1 && systemctl restart kubelet, node by node, draining each first\./g,"kubectl get nodes 를 확인하세요 — VERSION 열은 아직 예전 버전입니다! 이 열은 각 노드의 kubelet 버전이고, kubeadm은 kubelet을 건드리지 않습니다: 노드마다 drain 후 apt-get install -y kubelet=$1-1.1 && systemctl restart kubelet."],
 [/Config updated — the kubelet binary is still old\. Finish with: apt-get install -y kubelet=(.+?)-1\.1 && systemctl restart kubelet \(node drained, right\?\), then uncordon\./g,"설정 갱신 완료 — kubelet 바이너리는 아직 예전 것입니다. 마무리: apt-get install -y kubelet=$1-1.1 && systemctl restart kubelet (노드는 drain 했겠죠?), 그다음 uncordon."],
 ["You restarted the kubelet on a node that was NOT drained — its pods briefly ran unsupervised. Exam habit: kubectl drain NODE first, uncordon after.","drain 하지 않은 노드에서 kubelet을 재시작했습니다 — 그 파드들이 잠시 무감독 상태였어요. 시험 습관: 먼저 kubectl drain 노드, 끝나면 uncordon."],
 ["Heads up: this kubelet jumped versions without 'kubeadm upgrade node' — its node config is still the old generation. Run it (order: kubeadm upgrade node, THEN restart the kubelet).","주의: 이 kubelet은 'kubeadm upgrade node' 없이 버전을 건너뛰었습니다 — 노드 설정이 아직 예전 세대예요. 실행하세요 (순서: kubeadm upgrade node, 그다음 kubelet 재시작)."],
 ["etcd (and its pki files) live on the control-plane — ssh there.","etcd(와 그 pki 파일)는 컨트롤 플레인에 있습니다 — 그쪽으로 ssh 하세요."],
 [/etcd serves TLS and verifies the CLIENT too — this fails without (.+?)\. The exam-standard flags: --endpoints=https:\/\/127\.0\.0\.1:2379 --cacert=(.+?)ca\.crt --cert=(.+?)server\.crt --key=(.+?)server\.key/g,"etcd는 TLS를 쓰고 클라이언트도 검증합니다 — $1 없이는 실패해요. 시험 표준 플래그: --endpoints=https://127.0.0.1:2379 --cacert=$2ca.crt --cert=$3server.crt --key=$4server.key"],
 [/etcd's certs live in (.+?) \(ca\.crt, server\.crt, server\.key\) — not the general pki dir\./g,"etcd의 인증서는 $1 에 있습니다(ca.crt, server.crt, server.key) — 일반 pki 디렉터리가 아니에요."],
 [/That file IS the whole cluster: every object \((\d+) right now\) at this instant\. Anything you break after this moment can be rolled back with snapshot restore\./g,"그 파일이 곧 클러스터 전체입니다: 이 순간의 모든 오브젝트(지금 $1개). 이후에 망가뜨리는 무엇이든 snapshot restore로 되돌릴 수 있습니다."],
 ["Restore unpacks into a NEW directory — pass --data-dir /var/lib/etcd-restore, then point the etcd static pod at it.","복구는 새 디렉터리에 풀립니다 — --data-dir /var/lib/etcd-restore 를 주고, etcd 스태틱 파드가 그곳을 보게 하세요."],
 [/In a real cluster you would now edit \/etc\/kubernetes\/manifests\/etcd\.yaml so its hostPath volume points at (.+?) — the kubelet restarts etcd, and the API server sees the OLD data\. The sim just did that part: watch the cluster state roll back →/g,"실제 클러스터라면 이제 /etc/kubernetes/manifests/etcd.yaml 의 hostPath 볼륨이 $1 을 가리키게 수정합니다 — kubelet이 etcd를 재시작하고 API 서버는 예전 데이터를 보게 되죠. 그 부분은 시뮬레이터가 대신 했습니다: 클러스터 상태가 되돌아가는 걸 보세요 →"],
 ["Leaf certs live 1 year (kubeadm renews them on every upgrade), CAs live 10. 'kubeadm certs renew all' would renew by hand; openssl x509 -in CERT -noout -dates inspects a single file.","리프 인증서 수명은 1년(kubeadm이 업그레이드 때마다 갱신), CA는 10년입니다. 'kubeadm certs renew all'은 수동 갱신, openssl x509 -in 인증서 -noout -dates 는 파일 하나를 들여다봅니다."],
 ["kubeadm issues leaf certs for 1 year — every kubeadm upgrade renews them, so clusters upgraded at least yearly never expire.","kubeadm은 리프 인증서를 1년짜리로 발급합니다 — 업그레이드 때마다 갱신되므로, 최소 1년에 한 번 업그레이드하는 클러스터는 만료를 겪지 않습니다."],
 ["CAs get 10 years.","CA는 10년을 받습니다."],
 ["The bulk view is: kubeadm certs check-expiration","한꺼번에 보려면: kubeadm certs check-expiration"],
 /* roadmap bar */
 ["Every expert started at 0.","모든 전문가는 0에서 시작했습니다."],
 ["Foundations first — Modules 1–2.","기초부터 — 컨테이너 101과 Docker 실습."],
 ["Solid. Kubernetes core is where it gets fun.","탄탄해요. 이제 쿠버네티스 핵심이 재미있어지는 구간입니다."],
 ["You'd pass most junior DevOps interviews now.","이제 주니어 DevOps 면접은 통과할 수준입니다."],
 ["Expert territory — GPUs and production await.","전문가 영역 — GPU와 프로덕션이 기다립니다."],
 ["🏆 Roadmap complete. Go run it for real.","🏆 로드맵 완주! 이제 실전에서 돌려보세요."],
];;

export function tr(lang,h){
  if(lang!=="ko"||h==null)return h;
  h=String(h);
  for(const[a,b]of KO_STR)h=a instanceof RegExp?h.replace(a,b):h.replaceAll(a,b);
  return h;
}
